import {mvp} from 'Utils';

const metrics = {
  padding_small: mvp(4),
  padding_compact: mvp(8),
  padding_medium: mvp(16),
  padding_extra: mvp(32),
  radius_medium: mvp(8),
  radius_large: mvp(16),
  radius_extra: mvp(25),
};

export {metrics};
