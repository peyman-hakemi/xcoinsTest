import {colorPalettes} from './colorPalettes';

const colors = {
  background: colorPalettes.black,
  exchangeBackground: colorPalettes.blackRussian,
  regularIcons: colorPalettes.greyLight,
  regularText: colorPalettes.greyLight,
};

export {colors};
